# Meanwhile CLI

This directory contains the CLI entrypoint for the Meanwhile runtime.

Status: scaffolded. Commands currently print their parsed arguments and exit.

Planned commands:
- `meanwhile version`
- `meanwhile run`
- `meanwhile start`

Layout:
- `cmd/meanwhile/` - CLI entrypoint
- `internal/` - CLI implementation packages
